# Design reference material — not part of the deployed app
design_handoff_xing_mandarin/

# Generated audio output (pinyin/sandhi TTS pipelines) — regenerated locally, never committed
out/
audio_out/
__pycache__/

# Secrets / local Supabase CLI state
.env
.env.*
supabase/.temp/

# Staging area for landing/paket integration work-in-progress
incoming/
_tmp_asset_check/

# Catatan kerja lokal, memuat hal yang tidak untuk publik. Repo ini publik.
docs/HANDOFF.md

# Konfigurasi lokal Claude Code + area buangan. Dua-duanya memang tidak pernah di-track;
# ditulis di sini supaya satu `git add -A` yang kelewat tidak memasukkannya ke repo yang
# PUBLIK ini. Isinya sudah dipindai: tidak ada kunci, tapi tetap bukan untuk umum.
.claude/
_to_delete/

# Keluaran audit dan percobaan gambar. Semuanya dihasilkan ulang dari skrip di scripts/,
# jadi bukan sumber kebenaran -- yang jadi sumber kebenaran itu isi database dan storage.
# Ditulis di sini karena alasan yang sama dengan blok di atas: repo ini PUBLIK, dan satu
# `git add -A` yang kelewat akan menerbitkan 500 KB berkas kerja yang tidak dipakai siapa pun.
audit_vocab_hsk1-2.csv
vocab_gap_candidates.csv
clips.json
images.json
img_out/
img_review/
test_img/
